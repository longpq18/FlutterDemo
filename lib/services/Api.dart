import 'ApiUrl.dart';

class API {
  static String url = ApiUrl.Url;
  static String login = '${url}/login';
  static String products = '${url}/products';
  static String productDetail = '${url}/products';
}